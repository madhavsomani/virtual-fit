VF-9 mesh outputs are generated here at runtime.

Placeholder `.glb` files created by `scripts/generate-garment-mesh.mjs` are
intentionally not committed. The committed catalog manifest lives in
`data/garment-pipeline-jobs.json`.
